# .env.example - copy to .env and fill real values (DO NOT commit .env)

# Server
PORT=4000

# Database (MySQL / XAMPP)
# If you're using XAMPP and didn't set a root password, leave DB_PASS blank.
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=                # usually blank on XAMPP; if you set a root password, put it here
DB_NAME=Vira

# Twilio (for server-driven calls/SMS)
# Get these from your Twilio console. On trial accounts you can only call/ SMS verified numbers.
TWILIO_ACCOUNT_SID=ACdab310efc24c449fe6db79cf3861f2fc
TWILIO_AUTH_TOKEN=51935cf80402d74e7209839ff0844b0e
TWILIO_FROM_NUMBER=+916372491331   # Twilio phone number in E.164 format (e.g. +12025550123)

# Optional: toggle features
# Set to "true" to enable Twilio actions; server will still run if Twilio not configured.
ENABLE_TWILIO=true

# Logging / debug
NODE_ENV=development
